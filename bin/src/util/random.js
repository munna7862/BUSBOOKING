"use strict";
console.log(process.argv[1]);
/*
var path = require('path');

let strTempPath = __filename;
console.log("File Path is: "+strTempPath);

let m = module.filename;
console.log("Module is: "+m);

let PROJECT_FOLDER = strTempPath.substring(0, strTempPath.indexOf("Project1") + 8);
console.log("Project Folder Path is: "+PROJECT_FOLDER);

let sParentFolder = path.dirname(strTempPath).split(path.sep).pop();
console.log("Parent Folder is: "+sParentFolder)

let sTestCaseName = strTempPath.slice(strTempPath.lastIndexOf(path.sep)+1,strTempPath.toString().length-3);
console.log(strTempPath.lastIndexOf(path.sep))
console.log("Test Case Name is: "+sTestCaseName);

let TEST_DATA_FOLDER = path.join(PROJECT_FOLDER, "testdata", sParentFolder);

console.log(TEST_DATA_FOLDER);
*/
